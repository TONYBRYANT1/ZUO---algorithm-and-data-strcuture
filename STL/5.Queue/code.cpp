// C++ 中的标准模板库（STL）提供了 queue 类来实现队列数据结构。以下是 queue 类的一些常见操作：

// push(): 将元素插入队尾。
// std::queue<int> myQueue;
// myQueue.push(10); // 在队列尾部插入元素 10

// pop(): 移除队首的元素。
// myQueue.pop(); // 移除队列头部的元素

// front(): 返回队首元素的引用。
// int frontElement = myQueue.front(); // 返回队列头部的元素值

// back(): 返回队尾元素的引用。
// int backElement = myQueue.back(); // 返回队列尾部的元素值

// empty(): 检查队列是否为空。
// if (myQueue.empty()) 
// {
//     // 队列为空的处理
// }

// size(): 返回队列中元素的数量。
// int queueSize = myQueue.size(); // 返回队列中元素的数量
// 这些是 queue 类的基本操作，你可以根据自己的需要对队列进行操作。