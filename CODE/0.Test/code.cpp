#include<bits/stdc++.h>

using namespace std;

string arr[10];

int main()
{
    int arr[10];
    memset(&arr[0] , -1 , sizeof(arr));
    for(const auto& e : arr)
    {
        cout << e << endl;
    }

    return 0;
}